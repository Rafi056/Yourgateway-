## Packages
framer-motion | Essential for high-quality page transitions and interactive animations.

## Notes
- Ensure `@shared/schema` and `@shared/routes` contain the exact exports specified in the prompt.
- Wouter is used for routing.
- The UI uses Shadcn-compatible utility classes, relying on a custom CSS variable palette defined in index.css.
- The aesthetic is a premium, trustworthy "Education Portal" using deep blues, crisp whites, and elegant typography.
